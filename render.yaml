services:
  - type: web
    name: humblemorde-tech-bot
    env: node
    plan: free
    startCommand: npm start
    envVars:
      - key: PORT
        value: 3000
      - key: NODE_ENV
        value: production
      - key: BOT_PREFIX
        value: "!"
